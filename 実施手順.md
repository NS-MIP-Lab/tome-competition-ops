# 詳細設計書作成課題 ― 実施手順

理解度評価に基づく学習支援の介入タイミング推定に用いる、Web ベースの実験ツールです。

## ファイル構成

```
index.html          トップページ（問題 × 条件の選択）
design_doc.html     課題本体。問題に依存しない共通部分
data/
  catalog.js        問題の一覧。トップページのメニューはここから作られる
  pA_problem.js     ユークリッドの互除法 … ソースコードと設問
  pA_quiz.js                            … 理解度テストの4択
  pB_problem.js     二分探索
  pB_quiz.js
  pC_problem.js     挿入ソート
  pC_quiz.js
  pD_problem.js     エラトステネスのふるい
  pD_quiz.js
```

## 問題の一覧

問題IDは紙の資料の記号に合わせています。書き出しファイル名にも入ります。

| ID | 記号 | アルゴリズム | コード |
|---|---|---|---|
| `pA` | 問題 A | ユークリッドの互除法 | 24行 |
| `pB` | 問題 B | 二分探索 | 29行 |
| `pC` | 問題 C | 挿入ソート | 28行 |
| `pD` | 問題 D | エラトステネスのふるい | 29行 |

**ソースコードを差し替えたら、設問が参照する行番号がずれていないか必ず確認してください**（各 `pN_problem.js` の冒頭コメントに対応表があります）。空行を1つ足しただけでも、以降の設問がすべて別の行を指すようになります。

現在、`データ/テストコード案` 配下の `.c` はリポジトリ側のコードより古い版です（問題 A・B・D）。**リポジトリ側が正**です。

`index.html` と `design_doc.html` は `data` フォルダと同じ階層に置いてください。別の PC へ移すときはフォルダごとコピーします。

## 実施のしかた

`index.html` を**ダブルクリックで開いてください**。サーバもネット接続も不要です。

VSCode の Live Server（Go Live）は実施に使わないでください。ファイルを保存するたびに被験者の画面が自動でリロードされます。編集作業のときだけ使ってください。サーバ経由にしたい場合は、自動リロードのない `python -m http.server` をこのフォルダで起動します。

## 実施の流れ

1. `index.html` をダブルクリックして開く（ネット接続もサーバも不要）
2. 問題と条件を選ぶ
   - **学習用** … ヒントボタンあり
   - **検証用** … ヒントボタンなし
3. 学習用では、被験者ID を入力して「記録開始」を押す。不足デバイスがあれば画面に明記されるが、利用できる信号だけで記録は開始される
4. 「開始する」を押す。実施日は当日の日付が自動で入る
5. 開始を押すまで、左のソースコードは隠れている
6. Q1 → Q2 → … と進む。空欄が残っていると次へ進めない
   - 空欄のまま進もうとすると、未入力の欄が赤く示される
   - あわせて「**どうしても分からない場合は、その欄に `\` を入れて進んでください**」と案内が出る
   - 2問目以降は「**前に戻る**」で前の設問を見直せる。戻った設問の理解度テストは再出題されない
   - 「はじめに」画面には戻れない（経過時間の起点が揺らぐため）
7. 各設問の直後に4択の理解度テストが出る（Q6 の後は出ない）
   - 画面の**右半分だけ**を覆うので、左のソースコードを見ながら答えられる
   - 自分が書いた設計書は隠れる
   - **1問ずつ表示**され、選んで押すと次の問題へ進む。前の問題には戻れない
8. 「回答を終える」を押すと LSL 記録が終了し、完了画面になって回答ファイルが書き出される

### LSL 記録（学習用）

課題を開く前に LabRecorder の Remote Control Server（EnableRCS）を有効にし、次を実行してください。

```sh
python lsl/recorder_server.py
```

XDF はリポジトリ直下の `results/<問題ID>/` に、`<被験者ID>_<日時>.xdf` の名前で保存されます（例：`results/pA/S01_20260815_103000.xdf`）。

### 学習用のヒントボタン

被験者が赤い「ヒント」ボタンを押すと、画面全体が赤く光り「ヒントを要請しました」と約6秒間表示されます。**実験員への合図**であり、ヒントの内容は画面に出ません。実験員が口頭で介入してください。何度でも押せ、押した時刻と設問がすべて記録されます。

## 書き出されるファイル

完了時に、既定のダウンロードフォルダへ自動的に保存されます。

| ファイル | 内容 | 用途 |
|---|---|---|
| `design_pA_learning_S01_20260813_1030.json` | 全記録 | **正本。必ず保管** |
| `…_answers.csv` | 設計書の回答。1行1項目 | 採点 |
| `…_events.csv` | 時刻・ヒント要請・理解度テスト。1行1イベント | 解析・機械学習 |

ファイル名は「問題ID・条件・被験者ID・日時」の順です。

**用途が違うので CSV を2枚に分けています。** 設計書の回答は人手で採点する材料、イベントは解析に使う数値で、混ぜると1つの表の中で列の意味が変わってしまうためです。3ファイルはいずれも `問題ID`・`条件`・`被験者ID` の列を持つので、突き合わせられます。CSV を失っても JSON から作り直せます。

### `_events.csv` の中身

`種別` 列で行を選べば、そのまま解析に使えます。

| 種別 | 対象 | 連番 | 所要ミリ秒 | 補足 |
|---|---|---|---|---|
| `セッション` | 課題全体 | ― | 課題全体の所要時間 | ― |
| `設問滞在` | 設問ID | 訪問回数 | 滞在時間 | 中断した場合は `中断` |
| `ヒント要請` | 設問ID | 通番 | ―（1点の出来事） | ― |
| `理解度テスト` | 直前の設問 | 出題順 | 反応時間 | 問題ID・選択番号・正誤 |

各行に開始と終了の時刻が3形式（ISO・Unix ミリ秒・経過ミリ秒）で入ります。ヒント要請は押した瞬間の1点なので、開始と終了が同じ値になります。

**開始時刻**は「開始する」を押した瞬間、**回答終了時刻**は「回答を終える」を押した瞬間です。

### 時刻の記録形式

時刻は3つの形で記録されます。用途が違うので使い分けてください。

| 形式 | 例 | 用途 |
|---|---|---|
| ISO 文字列 | `2026-08-11T16:38:05+09:00` | 目視で確認する |
| Unix ミリ秒 | `1786513085000` | 他の記録との突き合わせ（Garmin の CSV、動画、実験ノート） |
| 経過ミリ秒 | `473000` | **解析に使う。**「開始する」を押した瞬間を 0 とする |

- `所要ミリ秒` が課題全体にかかった時間です
- ヒント要請の `経過ミリ秒` は「開始から何ミリ秒後に助けを求めたか」を表します
- 理解度テストは**問ごとに** `開始時刻`（表示した瞬間）と `終了時刻`（回答した瞬間）を記録し、その差を `反応時間ミリ秒` として持ちます。1問ずつ表示しているので、純粋な反応時間になります
- `設問滞在` は、どの設問にいつからいつまでいたかの記録です。「前に戻る」で同じ設問に何度も入れるため、**訪問ごとに1件**積まれます。`設問別合計` に設問ごとの訪問回数と合計滞在時間をまとめてあります
- 設問の滞在時間に**理解度テストの回答時間は含まれません**（テスト表示より前に滞在を閉じています）
- 実施中に再読み込みされた場合、その時点で開いていた滞在記録は `中断` の印が付き、滞在時間は空になります。合計にも加算されません
- Python の `time.time()` と比べるときは Unix ミリ秒を 1000 で割ってください

経過時間は単調時計から求めているので、実施中に PC の時刻補正が入っても値が飛びません。

**脳波・心拍・視線を LSL で収録する場合は、経過ミリ秒を使ってください。** LSL の時刻は Unix 時刻ではなく起動基準の時計なので、Unix 時刻では直接対応しません。開始時点を LSL 側でも記録しておけば、`LSL 上の時刻 ＝ 開始時点の LSL 時刻 ＋ 経過ミリ秒 ÷ 1000` で全イベントを LSL の時間軸に載せられます。

3ファイル以上を連続で保存するため、ブラウザが初回だけ「複数のファイルのダウンロード」の許可を尋ねることがあります。本番前に一度通して「許可」にしておいてください。

入力内容はブラウザにも自動保存されるので、誤って閉じても復帰できます。ただし**保存先の PC を離れると回収できない**ため、書き出したファイルを必ず回収してください。

### 「分からない」を表す記号

すべての欄を埋めないと次へ進めない仕様のため、**分からなかった欄には `\` を入れてもらいます**。空欄のまま進もうとしたときに画面へ案内が出ます。

解析の際は、**`\` は無回答として扱ってください**。書き写しや当てずっぽうで埋めた回答と区別できるようにするための記号です。学習用では、ヒントを何度も求めたうえで `\` になった箇所が、つまずきの所在を示す手がかりになります。

## 被験者を切り替えるとき

入力内容はブラウザに残ります。**消さずに次の被験者を始めると、前の人の回答が入力済みの状態で始まってしまいます。** 次の3か所のどこからでも消せます。

- 完了画面の「**データを消去して次の被験者へ**」（通常はこれを使う。消去したうえで、問題と条件を選ぶトップページへ戻ります）
- トップページに出る赤い枠の「**すべて消去する**」（残っている条件が一覧表示されます）
- 課題の「はじめに」画面に出る「**消去して新しい被験者で始める**」

いずれも消えるのはブラウザ内の入力内容だけで、**書き出したファイルは消えません**。

トップページを開いたときに赤い枠が出ていなければ、残っているデータはありません。実施前に毎回ここを確認してください。

## 問題を追加・差し替えるとき

### 4択問題だけを変えたい

`data/pN_quiz.js` を編集します。1行目と最後の行以外は JSON と同じ書き方です。`answer` は **0 から数える**正解番号（0＝1番目、3＝4番目）。配列を `[]` にすると、その設問の後には出題されません。

### 設問やソースコードを変えたい

`data/pN_problem.js` を編集します。設問が参照する行番号は、ファイル冒頭のコメントに書いてあります。**ソースコードを差し替えたら行番号の対応も必ず確認してください。**

### 新しい問題を追加したい

1. `data/pN_problem.js` と `data/pN_quiz.js` を作る（既存ファイルをコピーして中身を差し替える）
2. `design_doc.html` の `<script src>` に2行足す
3. `data/catalog.js` の一覧に1行足す

`design_doc.html` の本体（レンダラ・未入力チェック・ヒント・書き出し）には触れません。

## 注意

- 問題IDを記号にしているのは、URL にアルゴリズム名が出ると被験者に見えてしまうためです。画面にもアルゴリズム名は表示しません（書き出しには記録されます）
- **4択の理解度テストは4問題すべて【仮】**です。問題文が固まったら `data/pN_quiz.js` を差し替えてください。設計書の設問は4問題すべて実際の内容が入っています
- 問題データを読み込めない場合はエラーを表示し、先へ進めないようにしてあります
