# 詳細設計書作成課題 ― 実施手順

理解度評価に基づく学習支援の介入タイミング推定に用いる、Web ベースの実験ツールです。

## ファイル構成

```
index.html          トップページ（問題 × 条件の選択）
design_doc.html     課題本体。問題に依存しない共通部分
data/
  catalog.js        問題の一覧。トップページのメニューはここから作られる
  p5_problem.js     エラトステネスのふるい … ソースコードと設問
  p5_quiz.js                              … 理解度テストの4択
  p3_problem.js     挿入ソート
  p3_quiz.js
  p6_problem.js     カデイン法
  p6_quiz.js
```

`index.html` と `design_doc.html` は `data` フォルダと同じ階層に置いてください。別の PC へ移すときはフォルダごとコピーします。

## 実施のしかた

`index.html` を**ダブルクリックで開いてください**。サーバもネット接続も不要です。

VSCode の Live Server（Go Live）は実施に使わないでください。ファイルを保存するたびに被験者の画面が自動でリロードされます。編集作業のときだけ使ってください。サーバ経由にしたい場合は、自動リロードのない `python -m http.server` をこのフォルダで起動します。

## 実施の流れ

1. `index.html` をダブルクリックして開く（ネット接続もサーバも不要）
2. 問題と条件を選ぶ
   - **学習用** … ヒントボタンあり
   - **検証用** … ヒントボタンなし
3. 被験者ID を入力して「開始する」。実施日は当日の日付が自動で入る
4. 開始を押すまで、左のソースコードは隠れている
5. Q1 → Q2 → … と進む。空欄が残っていると次へ進めない
6. 各設問の直後に4択の理解度テストが全画面で出る（Q9 の後は出ない）
7. 「回答を終える」を押すと完了画面になり、ファイルが書き出される

### 学習用のヒントボタン

被験者が赤い「ヒント」ボタンを押すと、画面全体が赤く光り「ヒントを要請しました」と約6秒間表示されます。**実験員への合図**であり、ヒントの内容は画面に出ません。実験員が口頭で介入してください。何度でも押せ、押した時刻と設問がすべて記録されます。

## 書き出されるファイル

完了時に、既定のダウンロードフォルダへ自動的に保存されます。

| ファイル | 内容 |
|---|---|
| `design_p5_learning_S01_20260808_2340.json` | 全記録 |
| `design_p5_learning_S01_20260808_2340.csv`  | 同じ内容を1行1項目にしたもの |

ファイル名は「問題ID・条件・被験者ID・日時」の順です。記録される内容は、モード／アルゴリズム名／被験者ID／実施日／開始時刻／回答終了時刻／ヒント要請の時刻と設問／理解度テストの回答と正誤／各設問の最終的な回答です。

**開始時刻**は「開始する」を押した瞬間、**回答終了時刻**は「回答を終える」を押した瞬間なので、この差が実際の作業時間になります。

3ファイル以上を連続で保存するため、ブラウザが初回だけ「複数のファイルのダウンロード」の許可を尋ねることがあります。本番前に一度通して「許可」にしておいてください。

入力内容はブラウザにも自動保存されるので、誤って閉じても復帰できます。ただし**保存先の PC を離れると回収できない**ため、書き出したファイルを必ず回収してください。

## 被験者を切り替えるとき

入力内容はブラウザに残ります。**消さずに次の被験者を始めると、前の人の回答が入力済みの状態で始まってしまいます。** 次の3か所のどこからでも消せます。

- 完了画面の「**データを消去して次の被験者へ**」（通常はこれを使う。消去したうえで、問題と条件を選ぶトップページへ戻ります）
- トップページに出る赤い枠の「**すべて消去する**」（残っている条件が一覧表示されます）
- 課題の「はじめに」画面に出る「**消去して新しい被験者で始める**」

いずれも消えるのはブラウザ内の入力内容だけで、**書き出したファイルは消えません**。

トップページを開いたときに赤い枠が出ていなければ、残っているデータはありません。実施前に毎回ここを確認してください。

## 問題を追加・差し替えるとき

### 4択問題だけを変えたい

`data/pN_quiz.js` を編集します。1行目と最後の行以外は JSON と同じ書き方です。`answer` は **0 から数える**正解番号（0＝1番目、3＝4番目）。配列を `[]` にすると、その設問の後には出題されません。

### 設問やソースコードを変えたい

`data/pN_problem.js` を編集します。設問が参照する行番号は、ファイル冒頭のコメントに書いてあります。**ソースコードを差し替えたら行番号の対応も必ず確認してください。**

### 新しい問題を追加したい

1. `data/pN_problem.js` と `data/pN_quiz.js` を作る（既存ファイルをコピーして中身を差し替える）
2. `design_doc.html` の `<script src>` に2行足す
3. `data/catalog.js` の一覧に1行足す

`design_doc.html` の本体（レンダラ・未入力チェック・ヒント・書き出し）には触れません。

## 注意

- 問題IDを `p3` / `p5` / `p6` としているのは、URL にアルゴリズム名が出ると被験者に見えてしまうためです。画面にもアルゴリズム名は表示しません（書き出しには記録されます）
- 挿入ソートとカデイン法の設問は**すべて【仮】**です。修正案の設計書ができたら文言を差し替えてください
- 問題データを読み込めない場合はエラーを表示し、先へ進めないようにしてあります
